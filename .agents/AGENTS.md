# Custom Rules for CourseReg Workspace

## Automatic Git Workflow
The user has explicitly requested that ANY changes, updates, or fixes made to this codebase MUST be automatically committed and pushed to the GitHub repository: https://github.com/JRiteeshReddy/CourseReg.git.

**Instructions:**
- At the end of any task that modifies code, run `git add .`
- Run `git commit -m "<descriptive message>"`
- Run `git push origin master` (or current branch)
- Do not wait for the user to ask you to push. Always do it automatically before ending your turn.
